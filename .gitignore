# Personal Projects — meta repo
#
# Each project ships as its own repo on github.com/zesun33.
# Locally they live as sub-directories here but keep their own git
# history, so the parent must ignore them.
#
# Uncomment a line when you scaffold that standalone repo locally.

# ML systems practice
/cuda-gemm-optimization/
/cuda-memory-benchmark/
/parallel-computing-lab/
/resnet-tensorrt-bench/
/triton-flash-attention-lite/

# HW agent tooling
/hw-agent-tooling/
/eda-docker-images/
/eda-devcontainer/
# /gh-actions-for-hw/
/mcp-verilog/
/hw-agent-skills/
/mcp-cocotb/
/mcp-yosys/
/mcp-rtl-review/
/mcp-openroad/
/kernel-forge/
# /agentic-asic/

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/
.cursor/
*.iml
